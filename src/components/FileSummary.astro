---
import { Image } from "astro:assets";
const { image, title, description, link } = Astro.props;
---

<div
  class="flex flex-col md:flex-row gap-y-8 gap-x-8 bg-white drop-shadow-lg rounded-xs p-8"
>
  <div class="flex flex-col gap-y-4">
    <div
      class="h-24 w-24 md:h-32 lg:h-48 md:w-32 lg:w-48 bg-white rounded-xs drop-shadow-md p-4"
    >
      <Image src={image} alt="The Ottawa School File" class="w-full h-full" />
    </div>
  </div>

  <div class="flex flex-col gap-y-4">
    <div class="font-bold text-2xl font-mono uppercase">{title}</div>
    <p>{description}</p>
    <div class="flex-1 flex flex-col justify-end">
      <a href={link} class="text-blue-900 font-mono hover:underline hover:drop-shadow-[0_0_15px_rgba(30,58,138,0.5)] transition-all"
        >Read More →</a
      >
    </div>
  </div>
</div>
